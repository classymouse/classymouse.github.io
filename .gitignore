# Visual Studio
.vs/

# Build artifacts
temp_repo_build/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# OS
.DS_Store
Thumbs.db

# Addon zips — only track the current release versions; old ones accumulate on disk but
# should not be committed (too large, not needed in git history).
# Update the !exceptions below when version numbers change.
plugin.video.thecrew/*.zip
script.module.thecrew/*.zip
script.thecrew.artwork/*.zip
!plugin.video.thecrew/plugin.video.thecrew-3.0.11.zip
!script.module.thecrew/script.module.thecrew-3.0.9.8.zip
!script.thecrew.artwork/script.thecrew.artwork-3.1.0.zip
